{
  "id": "d779e69b",
  "title": "P3 Upstream sync: port cmux/tmux mux UX fixes; zellij/WezTerm best-effort",
  "tags": [
    "upstream-sync",
    "priority-3",
    "mux",
    "cmux",
    "tmux",
    "zellij",
    "wezterm",
    "pane",
    "tdd"
  ],
  "status": "todo",
  "created_at": "2026-05-04T20:18:17.117Z",
  "assigned_to_session": "019df837-8c4f-71ca-ae08-4b242436fa75"
}

# P3 Upstream sync: port cmux/tmux mux UX fixes; zellij/WezTerm best-effort

Parent: `TODO-356d8c41`
Ledger: `docs/analysis/upstream-sync-ledger-pi.md`

## Upstream commits

- `6e336fe` — `fix: preserve mux focus during subagent launch (#36)`
- `913dc9c` — `fix(zellij): use available space for subagent panes (#44)`
- `d99cd4b` — `Improves Windows support (#39)`

## Backend priority

Must support for acceptance:

- cmux
- tmux

Nice-to-have / best effort:

- zellij
- WezTerm

Windows support:

- Lowest-priority `TO_BE_PROCESSED` scope.
- Port low-risk compatibility pieces if small, such as `where.exe` command detection and safer ESM path handling.
- Do not require full Windows validation unless a Windows test environment is available.

## Goal

Improve pane-launch UX and compatibility without regressing local orchestration behavior.

## Required behavior

- Launching subagents should not unexpectedly steal focus in required backends where upstream provides a fix and local behavior permits it.
- Preserve local `focus` / tmux-detached behavior used by orchestration wrappers.
- zellij placement improvements should be ported if they can be adapted without destabilizing cmux/tmux support.

## Verification

Use TDD where practical, especially around pure helper functions and mux command generation.

Before closing:

- Run `npm run build`.
- Run `npm test`.
- Run mux integration tests for cmux/tmux where available.
- Run zellij/WezTerm tests only if the environment supports them.
- Run the full integration suite per the project test skill/process.
- Update ledger statuses for processed mux commits.

## Lightweight implementation plan

A full `define-spec → generate-plan → execute-plan` cycle is not required for this TODO. The existing TODO already defines the upstream commits, priority order, acceptance behavior, and verification requirements.

Recommended execution path:

1. Claim the TODO before implementation.
2. Inspect the three upstream commits and compare them against the local mux/subagent launch code.
3. Add or update focused regression tests first where practical, especially for mux command generation and focus-preservation behavior.
4. Port cmux and tmux UX fixes first; these are required for acceptance.
5. Preserve existing local `focus` semantics and tmux-detached behavior used by orchestration wrappers.
6. Best-effort port zellij placement and WezTerm/Windows-compatible low-risk pieces only if they adapt cleanly without destabilizing cmux/tmux behavior.
7. Run required verification: `npm run build`, `npm test`, available cmux/tmux mux integration tests, optional zellij/WezTerm tests when supported, and the full integration suite per the project test skill.
8. Update `docs/analysis/upstream-sync-ledger-pi.md` for the processed mux commits.
9. Close the TODO after verification passes and ledger statuses are updated.
