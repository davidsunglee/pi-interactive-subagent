{
  "id": "8c06b75d",
  "title": "P4 Upstream sync: manually port activity/status supervision and subagent_interrupt",
  "tags": [
    "upstream-sync",
    "priority-4",
    "status",
    "activity",
    "subagent_interrupt",
    "pane",
    "orchestration",
    "tdd"
  ],
  "status": "todo",
  "created_at": "2026-05-04T20:18:31.239Z"
}

# P4 Upstream sync: manually port activity/status supervision and subagent_interrupt

Parent: `TODO-356d8c41`
Ledger: `docs/analysis/upstream-sync-ledger-pi.md`

## Upstream commits

- `9f10962` — `feat: add configurable subagent status supervision and turn-only interruption`
- `269b485` — `feat(subagents): suppress stall steer messages for interactive subagents`
- `b4b0287` — `feat: replace subagent liveness polling with child activity state (#35)`

## Goal

Manually adapt upstream's child activity/status supervision model into this fork without regressing local headless, Claude, orchestration, blocked-state, transcript, or usage behavior.

## Intended upstream behavior to evaluate/port

- Child-written activity snapshots for Pi-backed pane children.
- Widget states such as `starting`, `active`, `waiting`, `stalled`, and fallback `running`.
- Stall/recovery supervision messages for autonomous children.
- `interactive` semantics for suppressing stall/recovery notifications for user-driven panes.
- `subagent_interrupt` as a turn-only Escape interrupt for Pi-backed pane children.

## Local decision on `interactive`

Adopt upstream-style `interactive` behavior when status supervision is ported:

- `interactive` affects only status supervision notifications.
- It does not affect pane focus.
- It does not affect auto-exit lifecycle.
- It does not suppress completion, failure, or `caller_ping`.
- Default should follow upstream if compatible:
  - `auto-exit: true` → autonomous → `interactive: false`
  - no `auto-exit` / `auto-exit: false` → user-driven → `interactive: true`

## Constraints

- Do not cherry-pick blindly; this area overlaps heavily with local observability/orchestration code.
- Preserve headless backend behavior and result shape.
- Preserve Claude pane/headless behavior.
- Preserve orchestration registry behavior, including blocked-task virtual rows and resume routing.
- Preserve transcript/usage updates in widget and final results.
- `subagent_interrupt` should initially target Pi-backed pane children only unless Claude/headless semantics are explicitly verified.

## Verification

Use TDD. This is substantial enough to justify define-spec / generate-plan / execute-plan on this child todo before implementation.

Before closing:

- Run `npm run build`.
- Run `npm test`.
- Run relevant pane, orchestration, headless, and Claude tests.
- Run the full integration suite per the project test skill/process.
- Update ledger statuses for these commits to `PROCESSED` or document any scope changes.
