{
  "id": "dd074bb7",
  "title": "Fix per-call systemPrompt composition for agents with bodies",
  "tags": [
    "subagents",
    "system-prompt",
    "pi",
    "claude",
    "tests"
  ],
  "status": "closed",
  "created_at": "2026-05-08T20:25:29.472Z"
}

## Completed

Implemented deterministic composition of agent bodies and per-call `systemPrompt` values in `resolveLaunchSpec()`:

- trims each prompt source;
- ignores empty/whitespace-only sources;
- joins non-empty agent body first, blank line, then per-call prompt;
- preserves `system-prompt: append|replace` as transport selection rather than composition selection.

Added/updated coverage for:

- `resolveLaunchSpec()` body-only, caller-only, body+caller, whitespace-only body/prompt, trimming, ordering, and no-duplication behavior;
- Claude headless argv and Claude pane command flags (`--append-system-prompt` / `--system-prompt`);
- Pi role-block and system-prompt artifact paths;
- slow opt-in headless integration coverage for both Pi and Claude children observing both prompt parts.

Fresh verification run by parent session after reviewing/restoring the coder subagent changes:

- `node --test test/orchestration/launch-spec.test.ts test/orchestration/claude-event-transform.test.ts test/orchestration/thinking-effort.test.ts` -> 65 passed, 0 failed
- `node --test test/orchestration/launch-spec.test.ts` -> 25 passed, 0 failed (after adding explicit whitespace-only agent-body coverage)
- `npm test` -> 566 passed, 0 failed
- `npm run typecheck` -> exit 0
- `npm run lint` -> exit 0
- `node --test test/integration/headless-prompt-composition.test.ts` -> exit 0; slow suites skipped because `PI_RUN_SLOW=false`
- `PI_RUN_SLOW=1 node --test test/integration/headless-prompt-composition.test.ts` -> 2 passed, 0 failed
- `git diff --check` -> exit 0
