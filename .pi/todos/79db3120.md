{
  "id": "79db3120",
  "title": "P4: Integrate `caller_ping` with orchestration flows",
  "tags": [
    "priority-4",
    "orchestration",
    "caller_ping",
    "human-in-the-loop",
    "recovery"
  ],
  "status": "closed",
  "created_at": "2026-04-22T14:29:04.851Z"
}

Implemented per spec `.pi/specs/2026-04-22-orchestration-lifecycle-expansion-design-v3.md`.

Closure evidence:
- `caller_ping` inside async orchestration now transitions tasks into explicit `blocked` state.
- Blocked notifications are surfaced structurally with `orchestrationId`, `taskIndex`, `taskName`, `sessionKey`, and `message`.
- Serial and parallel flows both preserve orchestration context across block/resume.
- Widget blocked-state rendering and blocked-task cancellation behavior are implemented.
- Verified with fresh typecheck and targeted orchestration/unit/integration/backend tests on 2026-04-23.

Non-blocking scope note:
- Initial-run blocked detection for Claude-backed children remains intentionally out of scope in v1 per the approved spec; this does not prevent closure.
