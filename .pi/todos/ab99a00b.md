{
  "id": "ab99a00b",
  "title": "P6: Add orchestration-level `subagent_resume` support",
  "tags": [
    "priority-6",
    "orchestration",
    "resume",
    "session-continuity",
    "subagent_resume"
  ],
  "status": "closed",
  "created_at": "2026-04-22T14:29:04.851Z"
}

Implemented per spec `.pi/specs/2026-04-22-orchestration-lifecycle-expansion-design-v3.md`.

Closure evidence:
- Orchestration registry tracks ownership by `sessionKey` and routes standalone `subagent_resume` completions back into the owning orchestration.
- `subagent_resume` supports the approved XOR API: exactly one of `sessionPath` or `sessionId`.
- Resume re-ingestion updates blocked slots, continues serial runs after successful resume, and finalizes parallel runs when all slots become terminal.
- Verified with fresh typecheck and targeted orchestration/unit/integration/backend tests on 2026-04-23.

Non-blocking scope note:
- Declarative orchestration task types for resume were explicitly dropped by the approved spec and are not required for closure.
